import React from 'react';
import ReactDOM from "react-dom";
import App from "./app";

const rooteElement = document.getElementById("root");

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>, rooteElement);